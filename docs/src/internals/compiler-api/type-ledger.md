<!-- Generated by `make doc-generate` from
     src/*.x and the compiler's definitions. Do not edit by hand. -->

> **Provisional API:** This reference documents current compiler behavior.
> Compatibility is not yet promised.

# `src/type-ledger.x`

The compiler's Var tag tables, projected from the ledger.

## Functions

| Function | Summary |
| --- | --- |
| [`Type.builtin_var_tags`](#Type.builtin_var_tags) | Returns the process-lifetime `Var` tag of each builtin type. |
| [`Type.var_tag_rows`](#Type.var_tag_rows) | Returns each constant-row tag's `(top mask bottom)` encoding row. |

### `Type`

<a id="Type.builtin_var_tags"></a>
#### Type.builtin_var_tags

`Map Type.builtin_var_tags(void)`

Returns the process-lifetime `Var` tag of each builtin type.

Source: `src/type-ledger.x:39`

<a id="Type.var_tag_rows"></a>
#### Type.var_tag_rows

`Map Type.var_tag_rows(void)`

Returns each constant-row tag's `(top mask bottom)` encoding row.

Source: `src/type-ledger.x:42`

## Design notes

`lib/var-tags.xmacro` holds the ledger. This unit is the one compiler
unit that imports it, so the units that include `type.x` do not load
the ledger. Both tables have process lifetime.
