<!-- Generated by `python3 tools/gen-api-reference.py --write` from
     lib/*.x and etc/header-symbols.xlisp. Do not edit by hand. -->

# `lib/autodiff.x`

Reverse-mode differentiation recorded on a runtime tape.

## Primary API

| Function | Summary |
| --- | --- |
| [`AdNode.add`](#AdNode.add) | Sum. |
| [`AdNode.compare`](#AdNode.compare) | Orders nodes by primal value, so `<` and `>` compare values. |
| [`AdNode.cos`](#AdNode.cos) | Cosine. |
| [`AdNode.div`](#AdNode.div) | Quotient. |
| [`AdNode.exp`](#AdNode.exp) | Exponential. |
| [`AdNode.log`](#AdNode.log) | Natural logarithm. |
| [`AdNode.mul`](#AdNode.mul) | Product. |
| [`AdNode.neg`](#AdNode.neg) | Negation. |
| [`AdNode.sin`](#AdNode.sin) | Sine. |
| [`AdNode.sqrt`](#AdNode.sqrt) | Square root. |
| [`AdNode.sub`](#AdNode.sub) | Difference. |
| [`AdNode.tanh`](#AdNode.tanh) | Hyperbolic tangent. |
| [`AdNode.var`](#AdNode.var) | Boxes a node for `Var` participation. |
| [`AdTape.backward`](#AdTape.backward) | Seeds `result` with adjoint 1 and propagates active adjoints to its operands. |
| [`AdTape.input`](#AdTape.input) | Records an input or constant. |
| [`AdTape.new`](#AdTape.new) | Creates an empty tape in the active `Scope`. |
| [`Var.adnode`](#Var.adnode) | Unboxes a node from a `Var` produced by `AdNode.var`. |

### `AdNode`

<a id="AdNode.add"></a>
#### AdNode.add

`AdNode AdNode.add(AdNode a, AdNode b)`

Sum.

Source: `lib/autodiff.x:83`

<a id="AdNode.compare"></a>
#### AdNode.compare

`int AdNode.compare(AdNode a, AdNode b)`

Orders nodes by primal value, so `<` and `>` compare values.

Source: `lib/autodiff.x:130`

<a id="AdNode.cos"></a>
#### AdNode.cos

`AdNode AdNode.cos(AdNode a)`

Cosine.

Source: `lib/autodiff.x:143`

<a id="AdNode.div"></a>
#### AdNode.div

`AdNode AdNode.div(AdNode a, AdNode b)`

Quotient.

Source: `lib/autodiff.x:113`

<a id="AdNode.exp"></a>
#### AdNode.exp

`AdNode AdNode.exp(AdNode a)`

Exponential.

Source: `lib/autodiff.x:146`

<a id="AdNode.log"></a>
#### AdNode.log

`AdNode AdNode.log(AdNode a)`

Natural logarithm.

Source: `lib/autodiff.x:152`

<a id="AdNode.mul"></a>
#### AdNode.mul

`AdNode AdNode.mul(AdNode a, AdNode b)`

Product.

Source: `lib/autodiff.x:103`

<a id="AdNode.neg"></a>
#### AdNode.neg

`AdNode AdNode.neg(AdNode a)`

Negation.

Source: `lib/autodiff.x:123`

<a id="AdNode.sin"></a>
#### AdNode.sin

`AdNode AdNode.sin(AdNode a)`

Sine.

Source: `lib/autodiff.x:140`

<a id="AdNode.sqrt"></a>
#### AdNode.sqrt

`AdNode AdNode.sqrt(AdNode a)`

Square root.

Source: `lib/autodiff.x:155`

<a id="AdNode.sub"></a>
#### AdNode.sub

`AdNode AdNode.sub(AdNode a, AdNode b)`

Difference.

Source: `lib/autodiff.x:93`

<a id="AdNode.tanh"></a>
#### AdNode.tanh

`AdNode AdNode.tanh(AdNode a)`

Hyperbolic tangent.

Source: `lib/autodiff.x:161`

<a id="AdNode.var"></a>
#### AdNode.var

`Var AdNode.var(AdNode node)`

Boxes a node for `Var` participation.

Source: `lib/autodiff.x:40`

### `AdTape`

<a id="AdTape.backward"></a>
#### AdTape.backward

`void AdTape.backward(AdTape tape, AdNode result)`

Seeds `result` with adjoint 1 and propagates active adjoints to its
operands. Zero adjoints do not invoke reverse callbacks. Earlier adjoints
on the tape are cleared first, so repeated calls do not accumulate.

Source: `lib/autodiff.x:70`

<a id="AdTape.input"></a>
#### AdTape.input

`AdNode AdTape.input(AdTape tape, double value)`

Records an input or constant. Read `adjoint` after `AdTape.backward`.

Source: `lib/autodiff.x:63`

<a id="AdTape.new"></a>
#### AdTape.new

`AdTape AdTape.new(void)`

Creates an empty tape in the active `Scope`.

Source: `lib/autodiff.x:46`

### `Var`

<a id="Var.adnode"></a>
#### Var.adnode

`AdNode Var.adnode(Var value)`

Unboxes a node from a `Var` produced by `AdNode.var`.

Source: `lib/autodiff.x:43`

## Public types

| Type | Kind | Summary |
| --- | --- | --- |
| [`AdNode`](#AdNode) | struct | One recorded value: its primal, its accumulated adjoint, and the closure that pushes that adjoint to the operands it came from. |
| [`AdTape`](#AdTape) | struct | A recording of `AdNode` operations; see the struct below. |

<a id="AdNode"></a>
### AdNode

`typedef struct AdNode { double value, adjoint; Func back; AdTape tape; } *AdNode`

One recorded value: its primal, its accumulated adjoint, and the closure
that pushes that adjoint to the operands it came from.

Source: `lib/autodiff.x:26`

<a id="AdTape"></a>
### AdTape

`typedef struct AdTape *AdTape`

A recording of `AdNode` operations; see the struct below.
The [Automatic Differentiation guide](../../guide/autodiff.md) explains
runtime tapes and the compile-time alternatives.

Source: `lib/autodiff.x:21`

## Design notes

This optional module is included explicitly; it is not part of the
implicit prelude. `AdTape` records every arithmetic operation on its
`AdNode` values as a closure that propagates an adjoint back to the
operands, so a data-dependent computation that `$ad.reverse()` cannot
transform statically still yields a gradient. Values box through `Var`
and each operation allocates a node, so the static decorators in
`autodiff.xmacro` remain the fast path.

## Tests and examples

`make verify` (`unittest/test-autodiff.x`) and `make examples` (`magic/autodiff`).
