unittest/compiler-fixtures/raise-invalid-pointer.x:4:3: type: raise detail type (* int) is not immutable
    raise %(invariant (value $value));
    ^^^^^
  note: use a numeric value, enum, Symbol, Atom, String, List, or Var

