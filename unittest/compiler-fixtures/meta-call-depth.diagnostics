unittest/compiler-fixtures/meta-call-depth.x:11:31: macro: 'md_deep' was not answered at compile time
    printf("%d\n", md_deep(4000));
                                ^
  note: reason: its compile-time form nested too deep and was stopped; the call stays and runs at run time

