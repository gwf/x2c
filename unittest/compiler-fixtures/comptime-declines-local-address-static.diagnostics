unittest/compiler-fixtures/comptime-declines-local-address-static.x:14:3: region: the address of 'value' can outlive the local storage it points into when stored into a static
    saved = &value;
    ^^^^^
  note: local storage ends when the function returns

