unittest/compiler-fixtures/raise-bare-key.x:4:22: parse: raise detail key must be a bare Symbol
    raise %(invariant (<owner> "probe"));
                       ^^^^^^^
  note: use raise %(code (key value)...);

