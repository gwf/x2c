/* auto-generated by x2c.  Do not edit! */

#include "meta-records.h"

struct MetaPoint mr_make(int x, int y){
  struct MetaPoint result ={
    .x = x, .y = y
  }
  ;
  return result;
}

int mr_sum(struct MetaPoint point){
  point.x += 1;
  return point.x + point.y;
}

int mr_probe(int seed){
  struct MetaPoint original = mr_make(seed, seed + 1);
  struct MetaPoint copy = original;
  copy.x = 20;
  int * field = & original.y;
  * field += 2;
  struct MetaPoint * address = & copy;
  address -> y = 30;
  return original.x * 1000 + original.y * 100 + copy.x * 10 + copy.y + mr_sum(original);
}

int main(int argc, char * * argv){
  x2c_initialize();
  (void) argv;
  printf("%d %d\n", 3840, mr_probe(argc + 2));
  return 0;
}

