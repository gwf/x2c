unittest/compiler-fixtures/comptime-declines-static-initializer-address.x:5:3: region: the address of 'value' can outlive the local storage it points into when stored into a static
    static int *saved = &value;
    ^^^^^^
  note: local storage ends when the function returns

