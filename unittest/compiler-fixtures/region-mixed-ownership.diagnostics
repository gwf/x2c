unittest/compiler-fixtures/region-mixed-ownership.x:12:14: region: a fresh allocation can outlive the region it was allocated in when returned
    $scope() { return make(choose); }
               ^^^^^^
  note: region opened at line 12

unittest/compiler-fixtures/region-mixed-ownership.x:19:5: region: 'value' can outlive the region it was allocated in when returned
      return value;
      ^^^^^^
  note: region opened at line 17

unittest/compiler-fixtures/region-mixed-ownership.x:27:5: region: 'value' can outlive the region it was allocated in when returned
      return identity(value);
      ^^^^^^
  note: region opened at line 25

unittest/compiler-fixtures/region-mixed-ownership.x:35:3: region: a fresh allocation can outlive the region it was allocated in when returned
    return pass(choose);
    ^^^^^^
  note: region opened at line 33

