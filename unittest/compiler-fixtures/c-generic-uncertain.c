/* auto-generated by x2c.  Do not edit! */

#include "c-generic-uncertain.h"

Var double_var(double);

Var int_var(int);

String Var_str(Var);

int main(void){
  x2c_initialize();
  int number = 1, * first = & number, * second = & number;
  Bits bits ={
    1
  }
  ;
  enum Color color = GREEN;
  const volatile int both = 1;
  const volatile int * pointer = & both;
  Var fraction = double_var(_Generic('a', int : 3.75, default : 7));
  Var whole = int_var(_Generic('a', int : 42, default : 7.5));
  Var character = int_var(_Generic((0, 'a'), char : 3.75, int : 7));
  Var qualified = int_var(_Generic(pointer, volatile const int * : 9, default : 0.5));
  printf("%s %s %s %s\n", Var_str(fraction), Var_str(whole), Var_str(character), Var_str(qualified));
  int variable = _Generic(color, enum Color : 10, default : 20);
  int constant = _Generic(RED, int : 11, default : 21);
  int promoted = _Generic(color + 0, default : 12);
  int size = _Generic(sizeof(int), size_t : 13, default : 23);
  int distance = _Generic(first - second, ptrdiff_t : 14, default : 24);
  int field = _Generic(bits.small, default : 15);
  printf("%d %d %d %d %d %d\n", variable, constant, promoted, size, distance, field);
  return 0;
}

