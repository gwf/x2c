/* auto-generated by x2c.  Do not edit! */

#include "meta-folding.h"

static List _4, _3;

static String _5, _0;

static Var _2, _1;

static int _init_guard_ = 0;

__attribute__((constructor)) static void _file_init_(void);

static int mf_width(String text, List extra);

static String mf_label(int n);

int mf_base = 10;

static int mf_offset(int x);

static int mf_shifted(int x);

Var Symbol_var(Symbol);

List cons(Var, List);

__attribute__((constructor)) static void _file_init_(void){
  x2c_initialize_protocols();
  if(_init_guard_) return;
  _init_guard_ = 1;
  _0 = String_new("row-");
  _1 = Symbol_var(2);
  _2 = Symbol_var(4);
  _3 = cons(_2, NULL);
  _4 = cons(_1, _3);
  _5 = String_new("abcd");
}

int mf_poly(int x){
  return x * x + 3 * x + 1;
}

int String_len(String);

int List_len(List);

static int mf_width(String text, List extra){
  return String_len(text) + List_len(extra);
}

Var String_var(String);

String int_str(int);

static String mf_label(int n){
  return String_join(NULL, cons(String_var(_0), cons(String_var(int_str(n)), NULL)));
}

static int mf_offset(int x){
  return x + mf_base;
}

static int mf_shifted(int x){
  return mf_offset(x) * 2;
}

int main(void){
  x2c_initialize();
  if(! _init_guard_) _file_init_();
  int seven = 7, one = 1;
  String text = _5;
  printf("poly    %d %d\n", 71, mf_poly(seven));
  printf("width   %d %d\n", 6, mf_width(text, _4));
  printf("label   %s\n", mf_label(4));
  printf("offset  %d %d\n", mf_offset(1), mf_offset(one));
  printf("shifted %d %d\n", mf_shifted(1), mf_shifted(one));
  return 0;
}

