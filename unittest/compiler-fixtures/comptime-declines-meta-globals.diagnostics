unittest/compiler-fixtures/comptime-declines-meta-globals.x:14:1: macro: this function cannot run at compile time
  meta static int dg_offset(int x) => x + dg_base;
  ^^^^
  note: reason: file-scope state not declared meta

