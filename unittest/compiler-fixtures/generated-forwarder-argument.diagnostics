unittest/compiler-fixtures/generated-forwarder-argument.x:9:3: type: cannot convert (* "Rec") to ("Array")
    return (int) Array.len(&rec);
    ^^^^^^
  note: the pointer types are unrelated: cast the expression to say so on purpose

