// The generated-name space is closed to source declarations.  The local
// declaration keeps the diagnostic in the parse pass, whose location is
// repo-relative; file-scope declarations are rejected by the same
// predicate from the collector pass.
#include "x2c.x"

int main(void) {
  int _x2c_lambda_0 = 2;
  printf("%d\n", _x2c_lambda_0);
  return 0;
}
