/* auto-generated by x2c.  Do not edit! */

#include "macro-hole-grouping.h"

static int positive(int value);

static int positive(int value){
  if(!(value > 0)) return 0;
  return value;
}

int main(void){
  x2c_initialize();
  int a = 3, b = 0;
  Point point ={
    5, 6
  }
  ;
  Point * pointer = & point;
  printf("%d %d\n", positive(- 3), positive(4));
  printf("%d %d %d %d\n", (1 + 2) * 2, -(1 + 2), !(1 + 2), 1 + 2);
  printf("%d %d %d %d\n", a * 2, - a, ! a, a);
  printf("%d %d %d\n", (* pointer).x, (int)(a = 0), (a = 0) ? 1 : 2);
  if(a == 0) b = a > 0 ? 7 : 8;
  printf("%d\n", b);
  printf("%d %d %d %d\n", (-(1 + 1)) * 2, -(-(1 + 1)), !(-(1 + 1)), -(1 + 1));
  return 0;
}

