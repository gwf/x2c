unittest/compiler-fixtures/raise-invalid-file.x:4:3: type: raise detail type ("File") is not immutable
    raise %(invariant (value $value));
    ^^^^^
  note: use a numeric value, enum, Symbol, Atom, String, List, or Var

limit: too many errors, stopping

