unittest/compiler-fixtures/region-pooled-ownership.x:8:3: region: a fresh allocation can outlive the region it was allocated in when returned
    return String.new(bytes);
    ^^^^^^
  note: region opened at line 6

unittest/compiler-fixtures/region-pooled-ownership.x:14:3: region: a fresh allocation can outlive the region it was allocated in when returned
    return String.malloc(size);
    ^^^^^^
  note: region opened at line 12

unittest/compiler-fixtures/region-pooled-ownership.x:20:3: region: a fresh allocation can outlive the region it was allocated in when returned
    return make_list(value);
    ^^^^^^
  note: region opened at line 18

unittest/compiler-fixtures/region-pooled-ownership.x:35:3: region: 'inner' is used after the region that allocated it ended
    List outer = cons(inner, NULL);
    ^^^^
  note: region opened at line 32

