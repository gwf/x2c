/* auto-generated by x2c.  Do not edit! */

#include "c-tagged-object.h"

struct Point origin ={
  1, 2
}
;

enum Mode mode = FAST;

int fill(struct Packet * packet){
  return packet -> size;
}

int main(void){
  x2c_initialize();
  struct Packet packet ={
    5
  }
  ;
  printf("%d %d %d %d\n", origin.x, origin.y, mode, fill(& packet));
  return 0;
}

