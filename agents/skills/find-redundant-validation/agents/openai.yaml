interface:
  display_name: "Find Redundant Validation"
  short_description: "Find source-checked redundant checks"
  default_prompt: "Use $find-redundant-validation to find and review likely redundant checks in these x2c files without editing source."
