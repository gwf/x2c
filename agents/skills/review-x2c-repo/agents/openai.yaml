interface:
  display_name: "Review x2c Repository"
  short_description: "Review the requested scope and verify findings"
  default_prompt: "Use $review-x2c-repo for a read-only review of the requested scope, or the whole repository when no narrower scope is named. Reproduce findings and state actual coverage."
